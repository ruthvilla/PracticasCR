/** Crea un fichero getPool.js que retorne un pool de conexiones con la base de datos.

Crea un archivo initDb.js que se ayude del pool que genera el fichero anterior para crear una base de datos con el nombre bootcamp con las siguientes tablas en MySQL:

Tabla de estudiantes: con las columnas nombre, apellido y año de nacimiento.

Tabla de direcciones: con las columnas calle y código postal.

Se supone que cada dirección pertenece a un estudiante, agrega la clave foránea que corresponda. Agrega los campos de auditoría necesarios.

Ayúdate de un fichero .env para almacenar el host, el usuario y la contraseña de la base de datos. Guarda también el nombre que le darás a la base de datos. Usa las variables de ese fichero donde corresponda.

Añade al fichero .gitignore aquellos archivos y carpetas que no deberían formar parte de un repositorio.

Recuerda instalar los módulos necesarios en la consola de Cooding Rooms.

*/
